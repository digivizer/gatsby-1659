{
  "name": "gatsby-webpack-css-bug",
  "private": true,
  "description": "Mini app to reproduce Webpack module import bugs",
  "version": "1.0.0",
  "author": "Mark Rickerby <me@maetl.net>",
  "dependencies": {
    "gatsby": "1.2",
    "gatsby-link": "^1.0.1",
    "gatsby-plugin-postcss-sass": "^1.0.1",
    "gatsby-plugin-react-helmet": "^1.0.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "start": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.5.2"
  }
}
